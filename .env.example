# Example environment configuration for trueblocks-dalleserver
# Copy to .env and adjust as needed.

# REQUIRED unless you intentionally skip image generation.
# Your OpenAI key (needed for prompt enhancement + image generation)
OPENAI_API_KEY=sk-your-openai-key

# Set to 1 to skip actual image generation (offline / fast tests)
TB_DALLE_SKIP_IMAGE=0

# Set to 1 to disable LLM enhancement step (use raw prompt)
TB_DALLE_NO_ENHANCE=0

# Timeouts (Go duration format). Increase if you experience frequent timeouts.
TB_DALLE_ENHANCE_TIMEOUT=60s
TB_DALLE_IMAGE_TIMEOUT=30s

# DALL·E quality parameter (standard|hd etc.)
DALLE_QUALITY=high
